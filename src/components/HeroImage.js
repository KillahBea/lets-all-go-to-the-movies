import React, { Component } from 'react'

class HeroImage extends Component {
  render() {
    return (
      <>
        {/* <div className="hero-image">
          {this.props.movies.poster}[Math.Floor(Math.random() * 20 )]}
        </div> */}
      </>
    )
  }
}

export default HeroImage
